[alias]
        co = checkout
        ci = commit
        br = branch
        fr = !"git fetch && git rebase"
        ls = log --pretty=format:\"%C(yellow)%h%Cred%d\\\\ %Creset%s%Cblue\\\\ [%cn]\" --decorate
        lg = !"git lg1"
